import 'package:flutter/material.dart';

/// Modern Black Matte Design - Professional App Color Palette
class AppColors {
  // Primary Brand Colors - Vibrant Purple/Blue
  static const Color primary = Color(0xFF6366F1);  // Indigo
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color primaryDark = Color(0xFF4F46E5);
  static const Color primaryGlow = Color(0x336366F1);
  
  // Accent Colors - Teal/Cyan
  static const Color accent = Color(0xFF06B6D4);
  static const Color accentLight = Color(0xFF22D3EE);
  static const Color accentDark = Color(0xFF0891B2);
  static const Color accentGlow = Color(0x3306B6D4);
  
  // Background Colors - True Black Matte
  static const Color background = Color(0xFF000000);  // Pure black
  static const Color backgroundElevated = Color(0xFF0A0A0A);  // Slightly elevated
  static const Color surface = Color(0xFF111111);  // Card surface
  static const Color surfaceLight = Color(0xFF1A1A1A);  // Lighter surface
  static const Color surfaceHover = Color(0xFF1E1E1E);  // Hover state
  
  // Text Colors - High Contrast
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA1A1AA);  // Zinc-400
  static const Color textTertiary = Color(0xFF71717A);  // Zinc-500
  static const Color textMuted = Color(0xFF52525B);  // Zinc-600
  
  // Semantic Colors - Modern Vibrant
  static const Color success = Color(0xFF10B981);  // Emerald-500
  static const Color successLight = Color(0xFF34D399);
  static const Color warning = Color(0xFFF59E0B);  // Amber-500
  static const Color warningLight = Color(0xFFFBBF24);
  static const Color error = Color(0xFFEF4444);  // Red-500
  static const Color errorLight = Color(0xFFF87171);
  static const Color info = Color(0xFF3B82F6);  // Blue-500
  static const Color infoLight = Color(0xFF60A5FA);
  
  // UI Elements
  static const Color divider = Color(0xFF27272A);  // Zinc-800
  static const Color border = Color(0xFF3F3F46);  // Zinc-700
  static const Color borderLight = Color(0xFF52525B);  // Zinc-600
  static const Color hover = Color(0xFF18181B);  // Zinc-900
  static const Color ripple = Color(0x1AFFFFFF);
  
  // Chat Bubbles - Modern Gradients
  static const Color userMessageStart = Color(0xFF6366F1);
  static const Color userMessageEnd = Color(0xFF8B5CF6);  // Violet
  static const Color aiMessageBg = Color(0xFF1A1A1A);
  static const Color aiMessageBorder = Color(0xFF27272A);
  
  // Glass Morphism
  static const Color glassBackground = Color(0x0DFFFFFF);
  static const Color glassBorder = Color(0x1AFFFFFF);
  
  // Shadows & Glows
  static const Color shadowDark = Color(0x40000000);
  static const Color shadowLight = Color(0x20000000);
  static const Color glow = Color(0x406366F1);
}
